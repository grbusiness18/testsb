'use strict';

module.exports = {
  optionalData: (F) => {
    return (data, callback) => {
      if (!callback) {
        callback = data;
        data = {};
      }

      F(data, callback);
    };
  }
};
